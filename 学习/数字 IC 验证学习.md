非阻塞赋值主要是为了模拟触发器的行为。

ctrl + v 进入列操作后，按s可直接删除并进行输入。列操作输入后按 . 可重复上一次的操作，也就是复制上一次的输入值。

ctrl + v 进入列操作后，按o可切换到下一行，按鼠标中键可以直接粘贴。

先输数字，在按yy，可以选择复制多少行。

u是撤销。

![[Pasted image 20260429153455.png|367]]

[[SystemVerilog]]